<h1>1219 Administration</h1>

<p>
	<%= link_to 'Categories', categories_url %>
</p>

<ul>
	<% @minutes.each do |minute| -%>
	<li>
		<%= link_to minute.name, minute_path(minute) %> 
		(<%= minute.categories.map{|c| c.name}.join(', ') %>)
	</li>
	<% end -%>
</ul>
